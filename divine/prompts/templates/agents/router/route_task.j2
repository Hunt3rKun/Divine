当前任务：为 DAG 节点选择执行智能体。

DAG 节点：
{{ node }}

可用执行智能体：
- recon：信息收集、目标概况、服务和入口点观察。
- web：Web 应用结构、端点、参数、认证状态和输入点分析。
- host：主机、端口、服务、版本和配置风险分析。
- report：报告整理和证据汇总。

要求：
- 如果任务目标不清，路由给 planner。
- 如果任务结果需要审计或归因，路由给 evaluator。
- 输出必须符合 Router 输出 JSON schema。
