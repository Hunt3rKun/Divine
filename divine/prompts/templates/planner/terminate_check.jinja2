你是一名渗透测试规划员。请评估当前测试进度，判断是否应该终止测试。

## 测试目标

{{ goal }}

## 当前黑板状态摘要

```json
{{ blackboard_summary | tojson(indent=2) }}
```

## 任务 DAG 统计

```json
{{ dag_stats | tojson(indent=2) }}
```

## 终止判断标准

**只有满足以下条件之一才应终止（terminate: true）：**
1. **目标已达成**：已成功获取 flag 或完成了核心目标
2. **完全无路可走**：所有任务都已执行，没有新的攻击方向可以尝试
3. **所有任务终态**：DAG 中所有任务都已完成或失败

**以下情况不应终止（terminate: false）：**
- 还有 pending 任务未执行
- 虽然当前发现不多，但还有新的攻击面可以探索
- 黑板数据为空只是因为还没执行到相关任务

## 输出格式

请严格返回以下 JSON 格式（不含 markdown 代码块）：

{
  "terminate": true 或 false,
  "reason": "终止或继续的原因说明"
}
