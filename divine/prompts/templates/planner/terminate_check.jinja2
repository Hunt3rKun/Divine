你是一名渗透测试规划员。请评估当前测试进度，判断是否应该终止测试。

## 测试目标

{{ goal }}

## 当前黑板状态摘要

```json
{{ blackboard_summary | tojson(indent=2) }}
```

## 任务 DAG 统计

```json
{{ dag_stats | tojson(indent=2) }}
```

## 终止判断标准

请考虑以下因素：
1. **目标完成度**：是否已达成核心测试目标？
2. **任务完成率**：已完成任务占总任务的比例是否足够高？
3. **发现价值**：已收集的发现是否足以生成有意义的报告？
4. **剩余可行任务**：是否还有未完成且可执行的高价值任务？
5. **资源消耗**：是否已耗尽合理的测试资源？

## 输出格式

请严格返回以下 JSON 格式（不含 markdown 代码块）：

{
  "terminate": true 或 false,
  "reason": "终止或继续的原因说明",
  "confidence": 0.0 到 1.0 之间的置信度
}
